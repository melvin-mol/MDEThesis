operation invariant(expression: Uppaal!Expression, position: Uppaal!Point): Uppaal!Invariant {
    var invariant = new Uppaal!Invariant;
    invariant.expression = expression;
    invariant.position = position;
    return invariant;
}